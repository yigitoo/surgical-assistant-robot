

	HOW TO INSTALL PACKAGE.
	Steps:
	1. Copy misa_ros to your catkin workspace (default catkin_ws/src)
	2. Open misa_ros/misa_robot_control/CMakeLists.txt
	3. Put # mark on the following lines:
		add_library(KinovaJointStateQueue src/API/KinovaJointStateQueue.cpp)
		add_executable(KinovaAPIService src/API/KinovaAPIService.cpp)
		add_executable(KinovaAPI src/API/KinovaAPI.cpp)
		target_link_libraries(KinovaAPIService ${catkin_LIBRARIES} -lm -ldl)
		target_link_libraries(KinovaAPI ${catkin_LIBRARIES} -lm -ldl)
	   So they become 
		#add_library(KinovaJointStateQueue src/API/KinovaJointStateQueue.cpp)

		#add_executable(KinovaAPIService src/API/KinovaAPIService.cpp)
		#add_executable(KinovaAPI src/API/KinovaAPI.cpp)

		#target_link_libraries(KinovaAPIService ${catkin_LIBRARIES} -lm -ldl)
		#target_link_libraries(KinovaAPI ${catkin_LIBRARIES} -lm -ldl)
	4. Save this file. 
	5. Open a terminal
	6. Type cd /catkin_ws/
	7. Type "catkin_make"
	8. After make resulted succesfully, go to /misa_robot_control/CMakelists.txt file again.
	9. Remove the # marks you previously typed.
	10. Go to terminal again,
	11. Type cd /catkin_ws/
	12. Type catkin_make
	13. There you go, the package is installed. Enjoy!
	
